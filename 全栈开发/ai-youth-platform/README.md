# AI少儿编程展示平台

这是一个基于Next.js和MongoDB的AI少儿编程展示平台，用于展示学生的编程作品、赛事荣誉和课程内容。

## 功能特点

- 🏆 **赛事展示** - 展示学生参加的各类编程赛事
- 💻 **作品展示** - 展示学生的编程作品（图片、视频、HTML等）
- 🏅 **荣誉展示** - 展示学生获得的荣誉证书
- 📚 **课程内容** - 提供编程课程视频资源
- 👥 **用户管理** - 支持学生、教师和管理员角色
- 🔐 **身份验证** - 基于JWT的安全认证系统
- 📱 **响应式设计** - 适配各种设备屏幕

## 技术栈

- **前端框架**: Next.js 14 (App Router)
- **样式**: Tailwind CSS
- **数据库**: MongoDB
- **认证**: JWT
- **语言**: TypeScript
- **图片存储**: Cloudinary
- **开发工具**: ESLint, Prettier

## 项目结构

```
ai-youth-platform/
├── src/
│   ├── app/                  # Next.js App Router页面
│   │   ├── api/              # API路由
│   │   │   ├── auth/         # 认证相关API
│   │   │   ├── competitions/ # 赛事API
│   │   │   ├── works/        # 作品API
│   │   │   ├── honors/       # 荣誉API
│   │   │   ├── courses/      # 课程API
│   │   │   └── admin/        # 管理员API
│   │   ├── competitions/     # 赛事页面
│   │   ├── works/            # 作品页面
│   │   ├── honors/           # 荣誉页面
│   │   ├── courses/          # 课程页面
│   │   ├── login/            # 登录页面
│   │   ├── register/         # 注册页面
│   │   ├── admin/            # 管理员页面
│   │   ├── layout.tsx        # 根布局
│   │   └── page.tsx          # 首页
│   ├── components/           # React组件
│   │   ├── Navbar.tsx        # 导航栏
│   │   ├── HeroSection.tsx   # 首页横幅
│   │   ├── ContentSection.tsx # 内容展示区
│   │   ├── CompetitionsList.tsx # 赛事列表
│   │   ├── WorksList.tsx     # 作品列表
│   │   ├── HonorsList.tsx    # 荣誉列表
│   │   ├── CoursesList.tsx   # 课程列表
│   │   ├── UploadButton.tsx  # 上传按钮
│   │   └── AdminDashboard.tsx # 管理面板
│   ├── lib/                  # 工具库
│   │   ├── mongodb.ts        # MongoDB连接
│   │   └── auth.ts           # 认证中间件
│   ├── models/               # 数据模型
│   │   ├── User.ts           # 用户模型
│   │   ├── Competition.ts    # 赛事模型
│   │   ├── Work.ts           # 作品模型
│   │   ├── Honor.ts          # 荣誉模型
│   │   └── Course.ts         # 课程模型
│   └── styles/               # 样式文件
│       └── globals.css       # 全局样式
├── public/                   # 静态资源
├── package.json              # 项目依赖
├── tailwind.config.js        # Tailwind配置
├── tsconfig.json             # TypeScript配置
├── .eslintrc.js              # ESLint配置
├── .prettierrc               # Prettier配置
└── next.config.js            # Next.js配置
```

## 安装与运行

### 环境要求

- Node.js 18.0 或更高版本
- MongoDB 4.4 或更高版本
- npm 或 yarn

### 安装步骤

1. 克隆项目
```bash
git clone <repository-url>
cd ai-youth-platform
```

2. 安装依赖
```bash
npm install
```

3. 设置环境变量
创建 `.env.local` 文件并添加以下变量：
```
# MongoDB
MONGODB_URI=mongodb://localhost:27017/ai-youth-platform

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Cloudinary (for file storage)
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
```

4. 安装MongoDB

#### 方法一：使用Chocolatey安装（推荐）
```bash
# 以管理员身份运行命令提示符，然后执行：
choco install mongodb -y
```

#### 方法二：从官网下载安装
访问 [MongoDB下载中心](https://www.mongodb.com/download-center/community) 下载Windows版本并安装。

#### 方法三：使用Docker运行
```bash
docker run -d -p 27017:27017 --name mongodb mongo:latest
```

5. 启动MongoDB服务

Windows服务安装完成后，MongoDB应该会自动启动。如果没有，请手动启动：
```bash
net start MongoDB
```

6. 启动开发服务器
```bash
npm run dev
```

7. 打开浏览器访问 [http://localhost:3000](http://localhost:3000)

## 部署指南

### Vercel部署（推荐）

1. 将代码推送到GitHub仓库
2. 在Vercel中导入项目
3. 在Vercel控制台中配置环境变量
4. 部署项目

### 自定义服务器部署

1. 构建项目
```bash
npm run build
```

2. 启动生产服务器
```bash
npm start
```

3. 使用PM2管理进程（推荐）
```bash
npm install -g pm2
pm2 start npm --name "ai-youth-platform" -- start
```

### Docker部署

1. 创建Dockerfile（如果不存在）
2. 构建Docker镜像
```bash
docker build -t ai-youth-platform .
```

3. 运行容器
```bash
docker run -p 3000:3000 --env-file .env.local ai-youth-platform
```

## 故障排除

### 常见问题

1. **MongoDB连接错误**
   - 确保MongoDB服务正在运行
   - 检查`.env.local`文件中的`MONGODB_URI`是否正确
   - 确认网络连接和防火墙设置

2. **依赖安装失败**
   - 尝试删除`node_modules`和`package-lock.json`
   - 重新运行`npm install`
   - 检查Node.js版本是否符合要求

3. **开发服务器启动失败**
   - 检查端口3000是否被占用
   - 查看终端输出的错误信息
   - 确认所有环境变量已正确设置

4. **API请求失败**
   - 检查API路由是否正确
   - 确认数据库连接正常
   - 查看浏览器控制台和服务器日志

### 调试技巧

1. 使用浏览器开发者工具检查网络请求
2. 查看终端输出中的错误信息
3. 使用`console.log`在代码中添加调试信息
4. 检查MongoDB数据库中的数据

## 用户角色

- **学生**: 可以浏览所有内容，上传自己的作品
- **教师**: 可以管理课程，审核学生作品
- **管理员**: 拥有所有权限，可以管理用户和内容

## API文档

### 认证相关

- `POST /api/auth/register` - 用户注册
- `POST /api/auth/login` - 用户登录

### 内容管理

- `GET /api/competitions` - 获取所有赛事
- `POST /api/competitions` - 上传新赛事

- `GET /api/works` - 获取所有作品
- `POST /api/works` - 上传新作品

- `GET /api/honors` - 获取所有荣誉
- `POST /api/honors` - 上传新荣誉

- `GET /api/courses` - 获取所有课程
- `POST /api/courses` - 上传新课程

### 用户管理

- `GET /api/admin/users` - 获取所有用户
- `PUT /api/admin/users` - 更新用户角色
- `DELETE /api/admin/users` - 删除用户

## 贡献

欢迎提交Issue和Pull Request来改进这个项目。

## 许可证

MIT License